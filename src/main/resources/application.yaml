spring:
  application:
    name: eureka-server

  security:
    basic:
      enabled: true
    user:
      name: admin-eureka-username
      password: admin-eureka-password

server:
  port: 8761

eureka:
    client:
    register-with-eureka: false
    fetch-registry: false
    instance:
    hostname: localhost